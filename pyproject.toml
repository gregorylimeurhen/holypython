[project]
name = "snekscript"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "pyinstaller>=6.21.0",
    "pytest>=9.0.3",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
